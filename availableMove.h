#ifndef AVAILABLE_MOVE_H
#define AVAILABLE_MOVE_H

#include "piece.h"

class AvailableMove {
    public:
        /*
        AvailableMove(Piece* origin, Movement *rules, int x, int y);

        Piece* origin;
        Movement *rules;

        int x, y;
        */
};

#endif
